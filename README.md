# Telegram Chat Bot با Gemini

یک چت‌بات ساده تلگرام که از OpenRouter API برای استفاده از مدل Gemini 2.0 Flash Exp استفاده می‌کند.

## ویژگی‌ها

- 💬 چت هوشمند با مدل Gemini
- 🧠 حفظ تاریخچه گفتگو برای هر کاربر
- 🔄 دستورات مختلف (/start, /help, /clear)
- 📦 ساختار ماژولار و تمیز

## نصب و راه‌اندازی

### روش 1: راه‌اندازی خودکار (پیشنهادی)

فقط یک دستور کافی است:

```bash
python setup.py
```

این دستور به صورت خودکار:
- ✅ نسخه Python را بررسی می‌کند
- ✅ فایل‌های ضروری را بررسی می‌کند
- ✅ تمام پکیج‌های مورد نیاز را نصب می‌کند
- ✅ تنظیمات را بررسی می‌کند

### روش 2: راه‌اندازی دستی

#### 1. نصب پکیج‌های مورد نیاز

```bash
pip install -r requirements.txt
```

#### 2. اجرای ربات

```bash
python main.py
```

## ساختار پروژه

```
royanic/
├── main.py                 # فایل اصلی و نقطه شروع
├── setup.py               # فایل راه‌اندازی و نصب خودکار
├── requirements.txt       # وابستگی‌های پروژه
├── README.md             # این فایل
└── bot/                   # پکیج اصلی ربات
    ├── __init__.py       # فایل اولیه پکیج
    ├── config.py         # تنظیمات و API keys
    ├── telegram_bot.py   # هندلر ربات تلگرام
    └── openrouter_client.py  # کلاینت OpenRouter API
```

## توضیح ماژول‌ها

### `main.py`
نقطه شروع برنامه که ربات را راه‌اندازی می‌کند.

### `bot/config.py`
شامل تمام تنظیمات و کلیدهای API:
- توکن ربات تلگرام
- کلید API OpenRouter
- نام مدل مورد استفاده

### `bot/openrouter_client.py`
ماژول ارتباط با OpenRouter API:
- ارسال درخواست به API
- دریافت پاسخ از مدل Gemini
- مدیریت خطاها

### `bot/telegram_bot.py`
هندلر اصلی ربات تلگرام:
- پردازش دستورات (/start, /help, /clear)
- پردازش پیام‌های کاربر
- مدیریت تاریخچه گفتگو
- ارسال پاسخ به کاربر

## دستورات ربات

- `/start` - شروع مجدد ربات
- `/help` - نمایش راهنما
- `/clear` - پاک کردن تاریخچه گفتگو

## نکات مهم

- ربات برای هر کاربر به صورت جداگانه تاریخچه گفتگو را نگه می‌دارد
- در صورت طولانی بودن پاسخ، پیام به بخش‌های کوچکتر تقسیم می‌شود
- تمام تنظیمات در فایل `bot/config.py` قابل تغییر است

## توسعه

برای افزودن ویژگی‌های جدید:
1. توابع جدید را در ماژول مناسب داخل پوشه `bot/` اضافه کنید
2. هندلرهای جدید را در `bot/telegram_bot.py` ثبت کنید
3. تنظیمات جدید را در `bot/config.py` قرار دهید

